[package]
name = "benchmark_ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]
name = "benchmark_ffi"

[dependencies]
prost = "0.12"

[build-dependencies]
prost-build = "0.12"

[workspace]
